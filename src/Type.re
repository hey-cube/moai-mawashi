type pointX = int; // 0 <= pointX <= horizontal
type pointY = int; // 0 <= pointY <= vertical
type point = (pointX, pointY);
type slope = float; // -10. <= slope <= 10.
type cycle = float; // 1つを除いて 30 < cycle < diagonal
type param = (slope, cycle);
type rotation = float;
